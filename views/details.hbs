
    <main>
      <h1>{{one.name}}</h1>
      <img class="cube" src={{one.image}}>
      <div class="details">
        <p><span>Description:</span> {{one.description}}</p>
        <p><span>Difficulty level:</span> {{one.level}}</p>
        <a class="btn" href="/products">Back</a>
      </div>
    </main>